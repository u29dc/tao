## DATA-003 Add generated-vault benchmark smoke in CI scripts

Status: done

### Objective
Ensure repository benchmark entrypoint always runs against generated fixtures.

### Scope
bench scripts and CI helper commands.

### Concrete Steps
1. Verify bench and budget scripts always call fixtures generation under vault/generated by default.
2. Add explicit assertions in scripts for resolved fixture vault path safety.
3. Emit fixture profile and seed metadata into benchmark reports.
4. Add smoke command to run quick benchmark subset for CI/local validation.

### Required Files and Locations
- scripts/bench.sh
- scripts/budgets.sh
- package.json

### Implementation Notes
Keep smoke benchmark runtime bounded for CI use.

### Dependencies
- DATA-001
- DATA-002

### Acceptance Criteria
- [x] Bench scripts fail when fixture root is outside repository.
- [x] Benchmark reports include profile and seed metadata.
- [x] A quick smoke benchmark command succeeds on fresh clone after build.
